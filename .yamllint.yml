extends: default

rules:
  document-start: disable
  indentation:
    spaces: 2
    indent-sequences: true
  line-length:
    max: 120
    level: error
  truthy:
    allowed-values:
      - "true"
      - "false"
    check-keys: true
