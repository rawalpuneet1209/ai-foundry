apiVersion: v1
kind: Service
metadata:
  name: ai-gateway
  namespace: ai-foundry
spec:
  selector:
    app: ai-gateway
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
